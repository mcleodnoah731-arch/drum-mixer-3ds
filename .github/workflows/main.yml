name: build-3ds

on:
  push:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    # Preinstalled 3DS dev environment (includes libctru + bannertool + makerom)
    container:
      image: ahoischen/3ds-dev:latest

    steps:
      - uses: actions/checkout@v4

      - name: Show tools (debug)
        run: |
          echo "DEVKITPRO=$DEVKITPRO"
          echo "DEVKITARM=$DEVKITARM"
          which arm-none-eabi-gcc || true
          which bannertool || true
          which makerom || true

      - name: Build 3dsx + cia
        run: |
          make
          make cia

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: DrumMixer-build
          path: |
            DrumMixer.3dsx
            DrumMixer.cia
            README.txt
